{
  "name": "@courvia/commerce-shopify",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "A study, not an integration: Shopify's Storefront API behind declared EngineCapabilities (catalog and availability, nothing else) plus the frozen ADR-024 CommerceService facade. No HTTP client, no token, no shop — code_complete, never sandbox_verified (ADR-029).",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {
    "@courvia/commerce-domain": "workspace:*",
    "@courvia/platform": "workspace:*"
  },
  "devDependencies": {
    "@courvia/config": "workspace:*",
    "vitest": "4.1.11"
  }
}
