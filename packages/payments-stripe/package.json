{
  "name": "@courvia/payments-stripe",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Stripe adapter for the PaymentProvider port. Owns gateway concerns only — never persistence.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@courvia/commerce-domain": "workspace:*"
  },
  "devDependencies": {
    "@courvia/config": "workspace:*"
  }
}
